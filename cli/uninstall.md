---
summary: "CLI reference for `edwin uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `edwin uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
edwin uninstall
edwin uninstall --all --yes
edwin uninstall --dry-run
```
